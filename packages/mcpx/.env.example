# EVM wallet private key for signing x402 payments (hex string with 0x prefix)
X402_EVM_PRIVATE_KEY=0x...

# Optional: EVM network (defaults to "base")
# X402_EVM_NETWORK=base-sepolia

# Optional: Solana wallet private key (base58 encoded)
# X402_SVM_PRIVATE_KEY=...

# Optional: Solana network
# X402_SVM_NETWORK=solana-devnet

# Server configuration
PORT=3000
HOST=127.0.0.1

# Payment receiver address (EVM or Solana)
PAY_TO_ADDRESS=0x...

# Facilitator URL (optional, defaults to public facilitator)
# FACILITATOR_URL=https://facilitator.payai.network
